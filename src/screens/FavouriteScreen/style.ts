import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    position: 'absolute',
    left: '40%',
  },
  text: {
    fontSize: 18,
    fontWeight: 'bold',
    marginVertical: 20,
    marginLeft: 25,
  },
  flatList: { gap: 10, paddingBottom: 20 },
});

export default styles;
