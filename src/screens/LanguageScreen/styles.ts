import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  languageContainer: {
    gap: 15,
    marginTop: 30,
    marginHorizontal: 20,
  },
  header: {
    position: 'absolute',
    left: '38%',
  },
});

export default styles;
