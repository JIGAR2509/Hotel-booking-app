import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  imageConatainer: {
    gap: 20,
    padding: 10,
  },
  images: {
    height: 200,
    width: 200,
  },
});

export default styles;
